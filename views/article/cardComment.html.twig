<div class="card col-12 mb-5" style="width: 240px;">
    <div class="card-body">
        <h4 class="card-title">{{ comment.user.username }}</h4>
        <p class="card-text">{{ comment.comment }}</p>
    </div>
</div>